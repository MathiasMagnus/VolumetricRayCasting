#ifndef INCLUDED_CAMERA_H
#define INCLUDED_CAMERA_H

#include <QVector3D>
#include <iostream>
#include <fstream>
#include <sstream>
#include <math.h>

#ifndef EPSILON
#define EPSILON 0.001
#endif

class Camera
{
public:
	Camera();
	Camera(QVector3D position, QVector3D viewDir, QVector3D viewUp, QVector3D viewSide);

	bool Orthonormalize();

	QVector3D GetPosition() { return m_position; }
	QVector3D GetViewDir() { return m_viewDir; }
	QVector3D GetViewUp() { return m_viewUp; }
	QVector3D GetViewSide() { return m_viewSide; }

	QVector3D m_position;
	QVector3D m_viewDir;
	QVector3D m_viewUp;
	QVector3D m_viewSide;
	QVector3D m_viewedPosition;
	float	m_fieldOfView;

};

#endif